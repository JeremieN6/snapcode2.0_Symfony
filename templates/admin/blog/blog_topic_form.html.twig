<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Générer un Article IA - SnapCode™ Agency</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card { box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); }
        .btn-primary { background-color: #007bff; border-color: #007bff; }
        .btn-primary:hover { background-color: #0056b3; border-color: #0056b3; }
    </style>
</head>
<body>
    <div class="container-fluid py-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1 class="h3 mb-0">
                        <i class="fa fa-magic"></i> Générateur d'Articles IA
                    </h1>
                    <a href="{{ path('admin', {'crudAction': 'index', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController'}) }}" 
                       class="btn btn-outline-secondary">
                        <i class="fa fa-arrow-left"></i> Retour aux Articles
                    </a>
                </div>
                
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">
                            <i class="fa fa-magic"></i> Configuration de l'article
                        </h3>
                    </div>
                <div class="card-body">
                    <form method="post" action="{{ path('admin_article_generate') }}" id="articleGeneratorForm">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="subject" class="form-label">Sujet de l'article *</label>
                                    <input type="text" 
                                           class="form-control" 
                                           id="subject" 
                                           name="subject" 
                                           required 
                                           placeholder="Ex: Optimisation SEO pour e-commerce"
                                           maxlength="200">
                                    <div class="form-text">
                                        Décrivez le sujet principal de l'article que vous souhaitez générer.
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="category" class="form-label">Catégorie *</label>
                                    <select class="form-select" id="category" name="category" required>
                                        <option value="">Sélectionnez une catégorie</option>
                                        {% for category in categories %}
                                            <option value="{{ category.id }}">{{ category.name }}</option>
                                        {% endfor %}
                                    </select>
                                    <div class="form-text">
                                        Choisissez la catégorie qui correspond le mieux au sujet.
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="row mt-3">
                            <div class="col-12">
                                <div class="alert alert-info">
                                    <i class="fa fa-info-circle"></i>
                                    <strong>Informations :</strong>
                                    <ul class="mb-0 mt-2">
                                        <li>L'article sera généré entre 500 et 600 mots</li>
                                        <li>Une image sera automatiquement ajoutée au milieu de l'article</li>
                                        <li>Le contenu sera optimisé pour le SEO</li>
                                        <li>L'article sera créé en brouillon (non publié)</li>
                                    </ul>
                                </div>
                            </div>
                        </div>

                        <div class="row mt-3">
                            <div class="col-12">
                                <button type="submit" class="btn btn-primary" id="generateBtn">
                                    <i class="fa fa-magic"></i> Générer l'Article
                                </button>
                                <a href="{{ path('admin', {'crudAction': 'index', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController'}) }}" 
                                   class="btn btn-secondary">
                                    <i class="fa fa-arrow-left"></i> Retour aux Articles
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de chargement -->
<div class="modal fade" id="loadingModal" tabindex="-1" aria-labelledby="loadingModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-body text-center">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Chargement...</span>
                </div>
                <h5 class="mt-3">Génération en cours...</h5>
                <p class="text-muted">L'article est en cours de génération. Cela peut prendre quelques secondes.</p>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('articleGeneratorForm');
    const generateBtn = document.getElementById('generateBtn');
    const loadingModal = new bootstrap.Modal(document.getElementById('loadingModal'));

    form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Validation
        const subject = document.getElementById('subject').value.trim();
        const category = document.getElementById('category').value;
        
        if (!subject || !category) {
            alert('Veuillez remplir tous les champs obligatoires.');
            return;
        }
        
        // Afficher le modal de chargement
        loadingModal.show();
        
        // Désactiver le bouton
        generateBtn.disabled = true;
        generateBtn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> Génération...';
        
        // Soumettre le formulaire
        form.submit();
    });
});
</script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
