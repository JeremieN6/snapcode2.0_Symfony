<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Articles de Blog - SnapCode™ Agency</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card { box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); }
        .btn-primary { background-color: #007bff; border-color: #007bff; }
        .btn-primary:hover { background-color: #0056b3; border-color: #0056b3; }
    </style>
</head>
<body>
    <div class="container-fluid py-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1 class="h3 mb-0">
                        <i class="fa fa-file-text"></i> Gestion des Articles de Blog
                    </h1>
                    <div>
                        <a href="{{ path('admin', {'crudAction': 'new', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController'}) }}" 
                           class="btn btn-success me-2">
                            <i class="fa fa-plus"></i> Ajouter un Article
                        </a>
                        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#generateArticleModal">
                            <i class="fa fa-magic"></i> Générer un Article IA
                        </button>
                    </div>
                </div>
                
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">
                            <i class="fa fa-file-text"></i> Liste des Articles
                        </h3>
                    </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Titre</th>
                                    <th>Catégories</th>
                                    <th>Auteur</th>
                                    <th>Statut</th>
                                    <th>Date de création</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for post in posts %}
                                <tr>
                                    <td>{{ post.id }}</td>
                                    <td>
                                        <strong>{{ post.title }}</strong>
                                        {% if post.isFavorite %}
                                            <span class="badge bg-warning ms-1">
                                                <i class="fa fa-star"></i> Vedette
                                            </span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% for category in post.categories %}
                                            <span class="badge bg-info me-1">{{ category.name }}</span>
                                        {% endfor %}
                                    </td>
                                    <td>{{ post.users ? post.users.email : 'N/A' }}</td>
                                    <td>
                                        {% if post.isPublished %}
                                            <span class="badge bg-success">Publié</span>
                                        {% else %}
                                            <span class="badge bg-secondary">Brouillon</span>
                                        {% endif %}
                                    </td>
                                    <td>{{ post.createdAt|date('d/m/Y H:i') }}</td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a href="{{ path('admin', {'crudAction': 'edit', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController', 'entityId': post.id}) }}" 
                                               class="btn btn-sm btn-outline-primary" title="Modifier">
                                                <i class="fa fa-edit"></i>
                                            </a>
                                            <a href="{{ path('article_show', {'slug': post.slug}) }}" 
                                               class="btn btn-sm btn-outline-info" title="Voir sur le site" target="_blank">
                                                <i class="fa fa-eye"></i>
                                            </a>
                                            <a href="{{ path('admin', {'crudAction': 'detail', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController', 'entityId': post.id}) }}" 
                                               class="btn btn-sm btn-outline-secondary" title="Détails">
                                                <i class="fa fa-info-circle"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="7" class="text-center text-muted">
                                        <i class="fa fa-inbox fa-2x mb-2"></i>
                                        <p>Aucun article trouvé</p>
                                        <a href="{{ path('admin', {'crudAction': 'new', 'crudControllerFqcn': 'App\\Controller\\Admin\\PostsCrudController'}) }}" 
                                           class="btn btn-primary">
                                            <i class="fa fa-plus"></i> Créer le premier article
                                        </a>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de génération d'article -->
<div class="modal fade" id="generateArticleModal" tabindex="-1" aria-labelledby="generateArticleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="generateArticleModalLabel">
                    <i class="fa fa-magic"></i> Générer un Article IA
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form method="post" action="{{ path('admin_article_generate') }}" id="modalArticleGeneratorForm">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="modalSubject" class="form-label">Sujet de l'article *</label>
                                <input type="text" 
                                       class="form-control" 
                                       id="modalSubject" 
                                       name="subject" 
                                       required 
                                       placeholder="Ex: Optimisation SEO pour e-commerce"
                                       maxlength="200">
                                <div class="form-text">
                                    Décrivez le sujet principal de l'article.
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="modalCategory" class="form-label">Catégorie *</label>
                                <select class="form-select" id="modalCategory" name="category" required>
                                    <option value="">Sélectionnez une catégorie</option>
                                    {% for category in categories %}
                                        <option value="{{ category.id }}">{{ category.name }}</option>
                                    {% endfor %}
                                </select>
                                <div class="form-text">
                                    Choisissez la catégorie appropriée.
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-info mt-3">
                        <i class="fa fa-info-circle"></i>
                        <strong>Informations :</strong>
                        <ul class="mb-0 mt-2">
                            <li>Article de 500-600 mots avec image automatique</li>
                            <li>Contenu optimisé SEO et créé en brouillon</li>
                            <li>Génération via OpenAI et Pexels</li>
                        </ul>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fa fa-times"></i> Annuler
                </button>
                <button type="submit" form="modalArticleGeneratorForm" class="btn btn-primary" id="modalGenerateBtn">
                    <i class="fa fa-magic"></i> Générer l'Article
                </button>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const modalForm = document.getElementById('modalArticleGeneratorForm');
    const modalGenerateBtn = document.getElementById('modalGenerateBtn');

    modalForm.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Validation
        const subject = document.getElementById('modalSubject').value.trim();
        const category = document.getElementById('modalCategory').value;
        
        if (!subject || !category) {
            alert('Veuillez remplir tous les champs obligatoires.');
            return;
        }
        
        // Désactiver le bouton
        modalGenerateBtn.disabled = true;
        modalGenerateBtn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> Génération...';
        
        // Soumettre le formulaire
        modalForm.submit();
    });
});
</script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
